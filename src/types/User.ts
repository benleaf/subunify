export type User = {
    id: string
    firstName: string
    lastName: string
    about: string
    tagLine: string
    zipCode: string
    email: string
    color: string
    thumbnail: string
    stripeSubscriptionId: string
    email_verified: boolean
}